<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Document</title>
</head>
<body>
    <div class="phone">
        <div class="phone-battery">
            <progress class="phone-battery-bar" value="<%= battery%>" max="100"></progress>
        </div>
        <div>
            <p>Név: <%= name %></p>
            <p>Típus: <%= type %> </p>
            <p>Szín: <%= color %> </p>
            <p>Ár: <%= price %> </p>
            <p>Állapot: 
                <% if (condition === 'new') { %>
                 Új
                <% } else if (condition === 'mintCondition') { %>
                    Újszerű
                <% } else { %>
                    Karcos
                <% } %>    
            </p>
        </div>
    </div>
</body>
</html>